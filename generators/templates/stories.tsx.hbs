import { Meta, StoryObj } from '@storybook/react';

import {{pascalCase name}}, { Props } from '.';

export default {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}},
  argTypes: {},
} as Meta;

type Story = StoryObj<typeof {{pascalCase name}}>;

export const Default: Story = (args: Props) => <{{pascalCase name}} {...args} />;

Default.args = {};
